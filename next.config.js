/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "avada.website",
      "www.notion.so",
      "images.ctfassets.net",
      "miro.medium.com",
    ],
  },
};

module.exports = nextConfig
